<Window x:Class="BlockManager.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlockManager.UI"
        xmlns:converters="clr-namespace:BlockManager.UI.Converters"
        xmlns:vm="clr-namespace:BlockManager.UI.ViewModels"
        mc:Ignorable="d"
        Title="块库浏览器 - BlockManager" 
        Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- 转换器资源 -->
        <converters:Base64ToImageConverter x:Key="Base64ToImageConverter"/>
        <converters:IconConverter x:Key="IconConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        
        <!-- TreeView样式 -->
        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <ToolBar Grid.Row="0" ToolBarTray.IsLocked="True">
            <Button Command="{Binding LoadLibraryCommand}" ToolTip="加载块库">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📁" FontSize="16" Margin="0,0,5,0"/>
                    <TextBlock Text="加载块库"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button Command="{Binding RefreshCommand}" ToolTip="刷新">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄" FontSize="16" Margin="0,0,5,0"/>
                    <TextBlock Text="刷新"/>
                </StackPanel>
            </Button>
        </ToolBar>
        
        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" MinWidth="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：文件树 -->
            <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="1" Margin="5">
                <Grid>
                    <TreeView Name="FileTreeView" 
                              ItemsSource="{Binding RootNode.Children}"
                              SelectedItemChanged="TreeView_SelectedItemChanged"
                              MouseDoubleClick="TreeView_MouseDoubleClick">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <Image Width="16" Height="16" 
                                           Source="{Binding IconType, Converter={StaticResource IconConverter}}"
                                           Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                    
                    <!-- 加载指示器 -->
                    <Border Background="White" 
                            Opacity="0.8"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="⏳" FontSize="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="正在加载..." HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            
            <!-- 分隔条 -->
            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          Background="LightGray"
                          ResizeBehavior="PreviousAndNext"/>
            
            <!-- 右侧：预览区域 -->
            <Border Grid.Column="2" BorderBrush="LightGray" BorderThickness="1" Margin="5">
                <Grid>
                    <!-- 预览图片 -->
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                                  VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding CurrentPreview, Converter={StaticResource NullToVisibilityConverter}}"
                                  CanContentScroll="True">
                        <Image Name="PreviewImage" 
                               Stretch="Uniform"
                               Source="{Binding CurrentPreview.PreviewImageBase64, Converter={StaticResource Base64ToImageConverter}}"
                               Margin="10"/>
                    </ScrollViewer>
                    
                    <!-- 无预览时的提示 -->
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Visibility="{Binding CurrentPreview, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}">
                        <TextBlock Text="🖼️" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                        <TextBlock Text="选择文件查看预览" 
                                   HorizontalAlignment="Center" 
                                   FontSize="14" 
                                   Foreground="Gray"
                                   Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- 底部状态栏 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="连接状态: "/>
                    <TextBlock Text="已连接" Foreground="Green" FontWeight="Bold"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
